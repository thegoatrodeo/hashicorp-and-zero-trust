version: "2"
containers:
- name: cloud-client
  image: gcr.io/instruqt/cloud-client
  shell: /bin/bash
  ports:
  - 80
aws_accounts:
- name: awsaccount
azure_subscriptions:
- name: azuresubscription
  roles:
  - Contributor
